DB_HOST=localhost
DB_PORT=5432
DB_NAME=food_rescue
DB_USER=postgres
DB_PASSWORD=your_password
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=1h

# Optional: Render / production can use a single URL instead of the fields above
# DATABASE_URL=postgresql://user:password@host:5432/food_rescue
